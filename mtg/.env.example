# Optional: deck review AI (local dev only — uses Vite proxy)
VITE_OPENAI_API_KEY=

# Assistant tab (Groq) — local dev via Vite proxy; production on Vercel
GROQ_API_KEY=

# GitHub Pages builds: point at a Vercel deployment that has GROQ_API_KEY set
# VITE_GROQ_API_BASE=https://edhhelp.vercel.app
